package team.GunsPlus.Enum;

import team.ApiPlus.API.Effect.EffectType;

public enum GunEffectType implements EffectType{

	EXPLOSION(), LIGHTNING(), SMOKE(), POTION(), PUSH(), DRAW(), PLACE(), BREAK(), SPAWN(), FIRE(), NONE(); 
	
}